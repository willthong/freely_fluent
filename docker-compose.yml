services:
  app:
    image: ghcr.io/earendil-works/freely-fluent:${TAG:-latest}
    ports:
      - "8000:8000"
    environment:
      - BRAVE_SEARCH_API_KEY=${BRAVE_SEARCH_API_KEY}
    volumes:
      - card_data:/app/data
    restart: unless-stopped

volumes:
  card_data:
